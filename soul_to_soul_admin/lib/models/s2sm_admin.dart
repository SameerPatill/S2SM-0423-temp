class Admin {
  int adminId;
  String firstName;
  String lastName;
  String username;
  bool isAuthenticated;

  Admin(this.adminId, this.firstName, this.lastName, this.username,
      this.isAuthenticated);
}
